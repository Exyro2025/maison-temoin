.brand {
  display: flex;
  align-items: center;
  gap: 12px;
  text-decoration: none;
}

/* HARD CAP: This stops the logo from expanding the header */
.logo {
  height: 32px !important; /* Forces the image to stay small */
  width: auto;
  display: block;
  flex-shrink: 0;
}

.brand-text {
  display: flex;
  flex-direction: column;
  line-height: 1.1;
}

.brand-name {
  font-size: 16px;
  font-weight: 600;
  color: #ffffff; /* Or your --text variable */
}

.brand-sub {
  font-size: 11px;
  opacity: 0.7;
  white-space: nowrap;
}


