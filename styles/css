.brand {
  display: flex;
  align-items: center;
  gap: 12px;
  text-decoration: none;
  /* Prevent logo from pushing the header taller */
  max-height: 40px; 
}

.logo {
  /* HARD CONSTRAINT: This prevents the 'overtaking' */
  height: 32px !important; 
  width: auto;
  display: block;
  flex-shrink: 0;
  object-fit: contain;
}

.brand-text {
  display: flex;
  flex-direction: column;
  justify-content: center;
  white-space: nowrap; /* Keeps 'Post-Close Stewardship' on one line if needed */
}


